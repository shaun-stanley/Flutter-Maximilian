class Transaction {
  final String id;
  final String title;
  final double amount;
  final DateTime date;

  // Constructer that creates named parameters that can be used to assign values.
  Transaction({this.id, this.title, this.amount, this.date});
}
